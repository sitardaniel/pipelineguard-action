# Deliberately-committed fixture for pipelineguard-action's own test workflow.
# This is AWS's own publicly documented example key (used throughout AWS's
# docs precisely because it is not a real credential), included here only to
# give gitleaks something deterministic to detect.
AWS_ACCESS_KEY_ID = "AKIAIOSFODNN7EXAMPLE"
